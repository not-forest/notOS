//! Memory definitions related to virtual memory management.
//!
//! Only supported for architectures with `CONFIG_HAS_VIRTUAL_MEMORY` flag
//! enabled in the configuration YAML.

/// Virtual Memory Address Representation.
///
/// 
pub struct VirtualAddress(usize);


